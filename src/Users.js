import React from 'react';
import ReactDOM from 'reactDOM';



export class Users extends React.component
{
  render{

    return();
    {
      <div>

      </div>
    }
  }
}

ReactDOM.render(
   
   var showusers = this.state.users.map((u) => {
     return <tr><td>{u.id}<u.name></td><td>{u.username};
   }

   

   )
)
